import { utils, ShaderSystem } from "@pixi/core";
import { syncUniforms } from "./syncUniforms.mjs";
function install(_core) {
  utils.deprecation("7.1.0", "install() has been deprecated, @pixi/unsafe-eval is self-installed since 7.1.0");
}
function selfInstall() {
  Object.assign(
    ShaderSystem.prototype,
    {
      systemCheck() {
      },
      syncUniforms(group, glProgram) {
        const { shader, renderer } = this;
        syncUniforms(
          group,
          shader.program.uniformData,
          glProgram.uniformData,
          group.uniforms,
          renderer
        );
      }
    }
  );
}
selfInstall();
export {
  install
};
//# sourceMappingURL=install.mjs.map
